{
  "name": "@spike-land-ai/spike-db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "build": "echo 'CF Worker — no build step required'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src/"
  },
  "dependencies": {
    "hono": "^4.12.4",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260301.1",
    "@vitest/coverage-v8": "4.0.18",
    "typescript": "^5.9.3",
    "vitest": "4.0.18",
    "wrangler": "^4.69.0"
  }
}
