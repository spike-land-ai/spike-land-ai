---
import { AudioPlayer } from '../../../../../src/core/block-website/ui/BlogCompatComponents';

interface Props {
  src: string;
  title?: string;
}

const { src, title } = Astro.props;
---
<div class="not-prose my-8">
  <AudioPlayer client:visible src={src} title={title} />
</div>
