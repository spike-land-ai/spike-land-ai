{
  "name": "@spike-land-ai/spike-edge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "dev": "wrangler dev",
    "dev:remote": "wrangler dev --remote",
    "build": "echo 'CF Worker — no build step required'",
    "deploy": "wrangler deploy --minify",
    "typecheck": "tsc --noEmit",
    "lint": "eslint --quiet index.ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@sentry/cloudflare": "^10.43.0",
    "hono": "^4.12.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260307.1",
    "@vitest/coverage-v8": "^4.0.18",
    "eslint": "^10.0.3",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18",
    "wrangler": "^4.71.0"
  },
  "source": "./index.ts"
}
